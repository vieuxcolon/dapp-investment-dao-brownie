# Base image
FROM node:18-slim

# Set working directory
WORKDIR /workspace/packages/frontend

# Copy package.json and install dependencies
COPY package.json ./
RUN npm install

# Copy all frontend files
COPY . .

# Expose port (aligned with docker-compose)
EXPOSE 5173

# Start frontend dev server
CMD ["npm", "run", "dev"]
